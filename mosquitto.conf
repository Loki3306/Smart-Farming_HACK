# Mosquitto Configuration for Smart Farming IoT
# Simple development configuration

# Listen on all interfaces
listener 1883 0.0.0.0

# Allow anonymous connections (development only)
allow_anonymous true

# Disable persistence for faster startup
persistence false

# Logging
log_dest stdout
log_type all
log_timestamp true

# Keep alive settings
max_keepalive 60
