# IoT System Architecture - Visual Guide

## System Overview

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        SMART FARMING IoT SYSTEM                         │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────┐
│   HARDWARE      │
│   (ESP32)       │
│                 │
│  ┌───────────┐  │
│  │ Moisture  │  │ GPIO 34
│  │  Sensor   │  │
│  └───────────┘  │
│                 │
│  ┌───────────┐  │
│  │   DHT11   │  │ GPIO 4
│  │  Temp/Hum │  │
│  └───────────┘  │
│                 │
│  ┌───────────┐  │
│  │    NPK    │  │ GPIO 35
│  │Potentiomtr│  │
│  └───────────┘  │
│                 │
│  ┌───────────┐  │
│  │   Water   │  │ GPIO 2
│  │   Pump    │  │
│  └───────────┘  │
└────────┬────────┘
         │
         │ WiFi
         │
         ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                          MQTT BROKER                                    │
│                         (Mosquitto)                                     │
│                                                                         │
│  Topics:                                                                │
│  • farm/telemetry  ← ESP32 publishes sensor data                       │
│  • farm/commands   → ESP32 subscribes for irrigation commands          │
└────────┬────────────────────────────────────────────────────────────────┘
         │
         │ MQTT Protocol
         │
         ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                      FASTAPI BACKEND                                    │
│                    (Python + FastAPI)                                   │
│                                                                         │
│  ┌───────────────────────────────────────────────────────────────────┐ │
│  │  iot_irrigation/                                                  │ │
│  │                                                                   │ │
│  │  ┌─────────────────┐                                             │ │
│  │  │ mqtt_client.py  │  ← Subscribes to farm/telemetry             │ │
│  │  └────────┬────────┘                                             │ │
│  │           │                                                       │ │
│  │           ↓                                                       │ │
│  │  ┌─────────────────┐                                             │ │
│  │  │   models.py     │  ← Validates data with Pydantic             │ │
│  │  └────────┬────────┘                                             │ │
│  │           │                                                       │ │
│  │           ↓                                                       │ │
│  │  ┌─────────────────────────────────────────────────────────────┐ │ │
│  │  │              router.py                                      │ │ │
│  │  │                                                             │ │ │
│  │  │  • Receives MQTT data                                       │ │ │
│  │  │  • Stores to memory (immediate)                             │ │ │
│  │  │  • Writes to DB (throttled 30s)                             │ │ │
│  │  │  • Broadcasts to WebSocket (throttled 3s)                   │ │ │
│  │  │  • Evaluates irrigation logic                               │ │ │
│  │  │  • Publishes MQTT commands                                  │ │ │
│  │  └─────────────────────────────────────────────────────────────┘ │ │
│  └───────────────────────────────────────────────────────────────────┘ │
│                                                                         │
│  Endpoints:                                                             │
│  • WS  /iot/ws/telemetry/{farm_id}  ← WebSocket for real-time data     │
│  • GET /iot/status                  ← System status                    │
│  • GET /iot/latest/{farm_id}        ← Latest sensor data               │
│  • POST /iot/command                ← Manual irrigation control        │
└────────┬────────────────────────────────────────────────────────────────┘
         │
         │ WebSocket (ws://)
         │
         ↓
┌─────────────────────────────────────────────────────────────────────────┐
│                      REACT FRONTEND                                     │
│                   (TypeScript + React)                                  │
│                                                                         │
│  ┌───────────────────────────────────────────────────────────────────┐ │
│  │  services/IoTService.ts                                           │ │
│  │                                                                   │ │
│  │  • Connects to WebSocket                                         │ │
│  │  • Auto-reconnect (exponential backoff)                          │ │
│  │  • Heartbeat ping/pong                                           │ │
│  │  • Event subscriptions                                           │ │
│  └────────┬──────────────────────────────────────────────────────────┘ │
│           │                                                             │
│           ↓                                                             │
│  ┌───────────────────────────────────────────────────────────────────┐ │
│  │  components/dashboard/LiveSensorGrid.tsx                          │ │
│  │                                                                   │ │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐         │ │
│  │  │ Moisture │  │   Temp   │  │ Humidity │  │   NPK    │         │ │
│  │  │  Card    │  │   Card   │  │   Card   │  │   Card   │         │ │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘         │ │
│  │                                                                   │ │
│  │  • Real-time updates (3s)                                        │ │
│  │  • Smooth animations (Framer Motion)                             │ │
│  │  • Live status badge                                             │ │
│  └───────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────┘
```

## Data Flow Diagram

### 1. Sensor Data Flow (ESP32 → Dashboard)

```
┌─────────┐
│  ESP32  │
│ Sensors │
└────┬────┘
     │ Read sensors every 5s
     ↓
┌─────────────────┐
│  Create JSON    │
│  {              │
│   moisture: 45, │
│   temp: 26.5,   │
│   humidity: 68, │
│   npk: 512      │
│  }              │
└────┬────────────┘
     │ MQTT Publish
     ↓
┌─────────────────┐
│  MQTT Broker    │
│ farm/telemetry  │
└────┬────────────┘
     │ MQTT Subscribe
     ↓
┌─────────────────┐
│  FastAPI        │
│  mqtt_client    │
└────┬────────────┘
     │ Validate with Pydantic
     ↓
┌─────────────────┐
│  router.py      │
│  handle_sensor  │
│  _data()        │
└────┬────────────┘
     │
     ├─→ Store in memory (immediate)
     │
     ├─→ Write to DB (every 30s)
     │   └─→ Supabase sensor_logs table
     │
     ├─→ Broadcast WebSocket (every 3s)
     │   └─→ All connected clients
     │
     └─→ Evaluate irrigation logic
         └─→ If moisture < 35%
             └─→ Publish MQTT command
                 └─→ farm/commands
                     └─→ ESP32 receives
                         └─→ Activate pump
```

### 2. WebSocket Communication (Backend ↔ Frontend)

```
┌──────────────────┐                    ┌──────────────────┐
│  React Frontend  │                    │ FastAPI Backend  │
│  IoTService.ts   │                    │   router.py      │
└────────┬─────────┘                    └────────┬─────────┘
         │                                       │
         │ 1. Connect WebSocket                  │
         ├──────────────────────────────────────→│
         │   ws://localhost:8000/iot/ws/...      │
         │                                       │
         │ 2. Accept connection                  │
         │←──────────────────────────────────────┤
         │   Status: Connected                   │
         │                                       │
         │ 3. Send initial data                  │
         │←──────────────────────────────────────┤
         │   { type: "initial_data", ... }       │
         │                                       │
         │ 4. Heartbeat ping                     │
         ├──────────────────────────────────────→│
         │   "ping"                              │
         │                                       │
         │ 5. Heartbeat pong                     │
         │←──────────────────────────────────────┤
         │   "pong"                              │
         │                                       │
         │ 6. Sensor update (every 3s)           │
         │←──────────────────────────────────────┤
         │   { type: "sensor_update", data: {...}}│
         │                                       │
         │ 7. Update UI                          │
         ├─→ LiveSensorGrid                      │
         │   └─→ Animate values                  │
         │                                       │
```

### 3. Irrigation Logic Flow

```
┌─────────────────┐
│ Sensor Data     │
│ moisture: 30%   │ ← Below threshold!
└────┬────────────┘
     │
     ↓
┌─────────────────────────────────┐
│ evaluate_irrigation_logic()     │
│                                 │
│ if moisture < 35%:              │
│   ✓ Condition met!              │
└────┬────────────────────────────┘
     │
     ↓
┌─────────────────────────────────┐
│ mqtt_client.publish_command()   │
│                                 │
│ Topic: farm/commands            │
│ Payload: {                      │
│   command: "WATER_ON",          │
│   farm_id: "farm_001"           │
│ }                               │
└────┬────────────────────────────┘
     │
     ↓
┌─────────────────────────────────┐
│ MQTT Broker                     │
│ Forwards to ESP32               │
└────┬────────────────────────────┘
     │
     ↓
┌─────────────────────────────────┐
│ ESP32 mqtt_callback()           │
│                                 │
│ Receives: WATER_ON              │
│ Action: digitalWrite(PUMP, HIGH)│
└────┬────────────────────────────┘
     │
     ↓
┌─────────────────────────────────┐
│ Water Pump Activated            │
│ 💧 Irrigation started           │
└─────────────────────────────────┘
     │
     │ (After watering)
     ↓
┌─────────────────────────────────┐
│ Moisture increases              │
│ moisture: 60%                   │
└─────────────────────────────────┘
```

## Throttling Strategy

```
┌──────────────────────────────────────────────────────────────┐
│                    MQTT Message Received                     │
│                    (Every 5 seconds from ESP32)              │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         ↓
          ┌──────────────────────────────┐
          │   Store in Memory            │
          │   (Immediate - No Throttle)  │
          └──────────────────────────────┘
                         │
          ┌──────────────┴──────────────┐
          │                             │
          ↓                             ↓
┌──────────────────┐          ┌──────────────────┐
│ Database Write   │          │ WebSocket Broadcast│
│ (Throttled)      │          │ (Throttled)        │
│                  │          │                    │
│ Interval: 30s    │          │ Interval: 3s       │
│                  │          │                    │
│ Why?             │          │ Why?               │
│ • Prevent DB     │          │ • Live feel        │
│   rate-limiting  │          │ • Smooth updates   │
│ • Reduce costs   │          │ • No UI lag        │
└──────────────────┘          └──────────────────┘
```

## Security Layers

```
┌─────────────────────────────────────────────────────────────┐
│                     Security Layers                         │
└─────────────────────────────────────────────────────────────┘

1. MQTT Layer
   ├─→ Optional authentication (username/password)
   ├─→ TLS/SSL encryption (port 8883)
   └─→ Topic-based access control

2. Backend Layer
   ├─→ Pydantic data validation
   ├─→ Environment-based credentials
   ├─→ CORS configuration
   └─→ Error handling (no crashes)

3. WebSocket Layer
   ├─→ Origin validation
   ├─→ Farm ID verification
   └─→ Connection limits

4. Frontend Layer
   ├─→ Auto-reconnect (prevents DOS)
   └─→ Input sanitization
```

## File Structure

```
Smart-Farming_HACK/
│
├── backend/
│   ├── iot_irrigation/              ← NEW IoT Module
│   │   ├── __init__.py
│   │   ├── models.py                ← Pydantic models
│   │   ├── mqtt_client.py           ← MQTT client
│   │   ├── router.py                ← FastAPI routes + WebSocket
│   │   ├── README.md                ← Documentation
│   │   └── esp32_example.ino        ← ESP32 code example
│   │
│   ├── app/
│   │   └── main.py                  ← Modified (includes IoT router)
│   │
│   └── requirements.txt             ← Already has paho-mqtt
│
├── client/
│   ├── services/
│   │   └── IoTService.ts            ← NEW WebSocket client
│   │
│   ├── components/
│   │   └── dashboard/
│   │       └── LiveSensorGrid.tsx   ← NEW Real-time display
│   │
│   └── pages/
│       └── Home.tsx                 ← Modified (includes LiveSensorGrid)
│
├── .env.example                     ← Modified (MQTT config)
├── IOT_IMPLEMENTATION_SUMMARY.md    ← NEW Summary
└── IOT_QUICK_START.md               ← NEW Quick start guide
```

---

**Legend:**
- `→` Data flow direction
- `←` Subscription/listening
- `✓` Condition met
- `💧` Action triggered
